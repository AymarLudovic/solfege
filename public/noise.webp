(This is a placeholder. You would replace this with an actual image file named `noise.webp` in your `public` directory, ideally a subtle noise texture to add grit. For the purpose of generating code, I cannot create binary content.)